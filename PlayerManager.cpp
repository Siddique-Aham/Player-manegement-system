#include "Player.h"
#include "PlayerManager.h"
#include <iostream>
using namespace std;

// Global variables
const int MAX = 12;
Player players[MAX];
int playerCount = 0;

// Definitions of addPlayer(), displayAll(), etc.
